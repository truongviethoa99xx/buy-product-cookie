.rounded-circle.img-fluid {
    width: 50%;
    height: 50%;
    border-radius: 50%;
    object-fit: cover;
    border: 1px solid #000 ;
}

.text-muted {
    min-width: 300px;
    border: none;
    outline: none;
}

.text-center {
    display: flex;
    text-align: center!important;
    align-items: center;
    align-content: center;
    justify-content: center;
    position: relative;
}

.icon-file {
    position: absolute;
    bottom: 10%;
    right: 28%;
}

.card.flex-card-cus {
    display: flex; 
}

/* list */
@import url("https://fonts.googleapis.com/css?family=Poppins:400,500,600,700&display=swap");

:root {
  --pink-color: #f2726a;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Poppins", sans-serif;
}

body {
  height: 100vh;
  background: linear-gradient(45deg, #415165, #8596aa);
}

.main-body .page-wrapper .page-header-title h4{
    color: #fff;
}

.card-item {
  width: 300px;
  height: 400px;
  border-radius: 10px;
  text-align: center;
  background-color: #242628;
  overflow: hidden;
  margin-bottom: 20px;
}

.card__img {
  width: 120px;
  height: 120px;
  overflow: hidden;
  transition: 0.25s;
  margin: 0 auto;
  transform: translateY(25px);
  border-radius: 50%;
  border: 4px solid var(--pink-color);
  cursor: pointer;
}

.card__img:hover {
  width: 100%;
  height: 100%;
  border-radius: unset;
  border: unset;
  transform: unset;
}

img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}

h2 {
  color: white;
  margin-top: 40px;
}

p {
  color: var(--pink-color);
}

.card__social a {
  text-decoration: none;
  color: white;
  margin: 25px 20px 40px;
  display: inline-block;
  font-size: 18px;
  transition: 0.25s;
}

.card__social a:hover {
  color: var(--pink-color);
}

.card__social a:hover .card__social-detail {
    display: block;
}

button {
  outline: none;
  border: none;
  color: white;
  background-color: transparent;
  border: 1px solid var(--pink-color);
  padding: 10px 20px;
  border-radius: inherit;
  cursor: pointer;
  transition: 0.25s;
}

button:hover {
  background-color: var(--pink-color);
}

.card__social-link {
    position: relative;
}

.card__social-detail {
    position: absolute;
    top: -30px;
    left: -24px;
    min-width: 60px;
    text-align: center;
    border-radius: 8px;
    border: 2px var(--pink-color) solid;
    display: none;
}

.card__social-detail-title {
    margin-bottom: 0;
    color: #FFF;
}

.btn--primary-color {
    background-color: var(--pink-color);
    float: right;
}
.btn--primary-color:hover {
    background-color: #eed5d3;
}
